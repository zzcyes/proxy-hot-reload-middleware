{
  "name": "proxy-hot-reload-middleware",
  "version": "1.0.0",
  "description": "Express middleware for hot-reloading proxy configurations",
  "files": [
    "*.js"
  ],
  "scripts": {
    "build": "rollup -c",
    "prepublishOnly": "pnpm run build",
    "publish-lib": "pnpm run build && cd lib && pnpm publish",
    "test": "vitest"
  },
  "keywords": [
    "express",
    "middleware",
    "proxy",
    "hot-reload"
  ],
  "author": "zzcyes",
  "license": "MIT",
  "dependencies": {
    "http-proxy-middleware": "^2.0.0"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^22.0.0",
    "@rollup/plugin-node-resolve": "^13.3.0",
    "rollup": "^2.75.0",
    "rollup-plugin-terser": "^7.0.2",
    "vitest": "^2.0.5"
  },
  "peerDependencies": {
    "express": "^4.0.0"
  }
}